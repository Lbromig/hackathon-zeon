# Open questions

Running, de-duplicated, prioritized list owned by the PM reanalyze pass. Blockers /
critical-path first. Each item: **ID**, question, **status**, owner, and one-line impact.
Status ∈ `OPEN` · `NEW-this-run` · `ANSWERED`. Answered items are kept (not deleted) with
evidence, then archived below once stale.

_Last reanalyzed: 2026-07-26T06:10Z._

## Active (critical path first)

| ID | Question | Status | Owner | Impact |
|----|----------|--------|-------|--------|
| Q-OT-1 | OT-One driver `connect()`/`_send()` are **still** `TODO` (transport not opened): `connect()` sets `self._conn = object()` and `_send()` returns `None` (re-verified this cycle in HEAD `8a52ec4`, unchanged — no new team commit this cycle; unchanged for a **sixth** cycle). `_execute` calls `ot.aspirate(...)` for real, so this is the one wired motion that still **no-ops on hardware** — the pipette never actually draws. `origin/feat/ot-one-serial-driver` exists but is **not merged** into `agent-loop-p0`. | OPEN (#1, room-only, **the LONE decider**, 6th cycle) | Lukas | **The reveal is still mimed — the single remaining blocker, now for a sixth review.** Every other item has converged, and this cycle a large new *dexterity* capability landed (Q-CAPOPS-1) while this room-only merge stood still again. The Opentrons aspiration is the narrative climax and the only step whose driver does nothing physical — `aspiration_ok` would judge a twin/telemetry value with no real aspirate behind it. Room-only: merge/cherry-pick the serial driver at the bench or the reveal is a no-op with a green check. |
| Q-CAPOPS-1 | **NEW this run.** The **dual-arm ratchet-unscrew — the TARGET rung's signature dexterity move — now exists in code and is thoroughly tested** (working tree, uncommitted). `core/motion/cap_ops.py` implements `plan_unscrew`/`grab_cap`/`ungrab_cap`/`unscrew_cap`: a *ratchet* that respects the tool cabling's inability to take a continuous 360° — turn +180° gripped, open, unwind −180° free, re-grip, repeat — with **net-zero wrist travel** so it is repeatable, and it **pre-flights every intermediate wrist angle against the J6 soft limit before moving**. 12 tests (`backend/tests/test_cap_ops.py`) assert the ratchet plan, repeatability, joint-limit refusal, and that it ends released + unwound. It is exposed via a new teach endpoint (`POST /api/teach/{id}/cap` with `action=grab/ungrab/unscrew`, `half_turns`) and a `CapTools.vue` panel. **Two gaps:** (1) it is **not wired into the auto `CHOREOGRAPHY`** — `_execute` still runs the FLOOR snap-cap (straight-up lift), so the ratchet is a manual teach-panel tool, not part of the end-to-end run; (2) it is **uncommitted** (Q-COMMIT-2). Who wires it into the TARGET choreography (or is it a driven-live-from-the-panel demo beat), and who commits it? | NEW-this-run (dexterity ceiling raised) | Dale | **A real advance toward the TARGET rung and a strong dexterity+verification fallback story even if the OT never draws** — a demo of the dual-arm ratchet-unscrew gated by a real `cap_removed` verdict is compelling on its own. But it is laptop/bench-buildable code that landed while the room-only OT beat (Q-OT-1) stood still, and it isn't in the end-to-end path yet. Decide its demo role and commit it. |
| Q-POSES-1 | `_execute` replays named taught poses; tests use a fixture library. **Stalled again this cycle** after last cycle's unstall: `data/teach_poses.json` is **unchanged at 8 poses** (mtime still 05:24Z, no new teaching this run) — right (6): `cap_grasp_approach`, `cap_grasp`, `tube_grasp_approach`, `tube_grasp`, `transport_safe`, `present_approach`; left (2): `tube_hold_approach`, `tube_hold`. Both arms are started but neither is finished. Still untaught: `cap_lift`, `cap_dropoff`, `cap_dropoff_retreat`, the `present_ot`/aspirate poses, and more left-arm poses — so `preflight` still refuses the full choreography. Who finishes the remaining poses on the two-arm + OT bench? | OPEN (8 taught, both arms; **no progress this cycle**) | Dale / Lukas | **Demo-day blocker, hardware-only — and it stopped moving this cycle** while new laptop-buildable dexterity code (Q-CAPOPS-1) landed instead. Finish the remaining poses (room-only, can't be pre-staged) and the floor path runs; the momentum from last cycle stalled. |
| Q-TWIN-COUPLING | **Both halves now exist in code.** **Motion→twin half: COMMITTED** (see Q-KIN-1) — the arm-FK loop (`8a52ec4`) writes each arm's live TCP into the twin at ~12 Hz, wired in `main.py`'s lifespan. **Reparent half: now WRITTEN** (working tree, uncommitted) — `uncap_aspirate.py` gains `_apply_twin_effect(act)` called from `_run_act`, and the `CHOREOGRAPHY` grip/release acts carry `attach=`/`to=` ids (`grip attach="tube_1" to="left_tool"`, `grip attach="tube_1_cap" to="right_tool"`, `release attach="tube_1_cap" to="dropzone"`, handover `grip attach="tube_1" to="right_tool"`), so a real grasp calls `wm.reparent(...)` in production for the first time. The ids are **entity-consistent** — `tube_1`/`tube_1_cap` are seeded by `core/calibration/pipeline.py:125`, `left_tool`/`right_tool`/`dropzone` exist in `definitions.py` — so the reparent will actually fire (guarded by `in wm.entities`; silent no-op only if the twin wasn't seeded). **NEW this cycle:** a dedicated test, `backend/tests/test_workflow_twin_effects.py`, now drives `_apply_twin_effect` over the `uncap`/`transport` choreography and asserts `grasp_secure` and `cap_removed` verdicts **turn true after the reparent** — the coupling is now proven end-to-end in test, not just written. So `grasp_secure` (tube parented to a `TOOL`) and `cap_removed`'s reparent clause **can now turn true from a real grasp** once committed + twin seeded. Only gap left, **unchanged for a second cycle: commit the reparent WIP** (still uncommitted). | OPEN (verification-critical; both halves written + now tested, reparent half **still uncommitted** — 2nd cycle) | Di / Lukas | **Nearly closed — Track C's *verification* half.** The verify loop is no longer structurally hollow for the grasp step, and now has a test proving the coupling flips the verdicts; the remaining action is purely mechanical (commit it), yet it went uncommitted a second cycle. If it's cut, the geometry predicates (`tube_aligned`, `cap_removed`-separation) are the honest fallback and are now world-correct in code (Q-CALIB-1). |
| Q-COMMIT-2 | **NEW this run.** No team commit landed this cycle (HEAD parent is still `8a52ec4`), yet the working tree holds a **substantial, well-tested pile of uncommitted code**: the ratchet-unscrew (`core/motion/cap_ops.py` + `test_cap_ops.py`, Q-CAPOPS-1), hand-guided **path teaching** (`core/motion/path_teach.py` with deadband+RDP simplification, `core/teach_paths.py`, `backend/app/services/path_recorder.py`), the twin-effect **reparent** WIP (`uncap_aspirate.py`, Q-TWIN-COUPLING) + its new test (`test_workflow_twin_effects.py`), teach-API/schema changes (`teach.py`, `schemas.py`), and frontend (`CapTools.vue`, `useTeach.ts`, `api/teach.ts`). All `py_compile`-clean. This re-opens the exact failure the project already paid for once (Q-COMMIT-1): real work a clean checkout doesn't have. When does this batch get committed onto `agent-loop-p0`? | NEW-this-run (#2, mechanical) | Team / Lukas | **A repeat of the WIP-loss risk.** Minutes to fix, high cost if the demo runs a clean checkout at hour 24. Commit the batch (it's the team's code — the PM does not stage it) so the dexterity + coupling work survives. |
| Q-KIN-1 | **ANSWERED this run.** The **arm-FK → twin kinematics loop** — `core/kinematics.py` (`pose_to_base_tcp`, `update_arm_tcp`), `backend/app/services/kinematics.py` (`Kinematics` thread @ `KIN_HZ=12`, `kinematics_once`), `backend/tests/test_kinematics.py`, and `main.py`'s `kinematics.start()` lifespan wiring — was **committed in `8a52ec4`** ("Vision system etc."). It reads `ArmDriver.get_pose()` for every connected arm and writes it into `{arm}_tcp.local`, moving the whole TCP→tool→`gripper_cam` chain in the twin every tick (no-ops until calibration built `{arm}_tcp` and arms are connected). The uncommitted-code risk from last run is closed. | ANSWERED (2026-07-26T05:40Z) | Lukas / Di | **Resolved — the twin's arms now genuinely move, on a clean checkout.** A showable win for the world map and honest for the geometry verifiers. Residual is now Q-TWIN-COUPLING's reparent half (written, uncommitted) — commit it with the pose work. |
| Q-CALIB-1 | Calibration runs end-to-end and publishes the twin. **This cycle the fixed-camera `world_frame` step was COMMITTED** (`cfa8aac`, last cycle's recommendation done): `core/calibration/world_board.py` defines a shared world frame from a two-tag board (`tag36h11` 210 & 211), `extrinsics.py::solve_world_cam` solves each fixed camera's `T_world_cam` against it (hardware-free solver + synthetic round-trip test `backend/tests/test_extrinsics.py`), and `pipeline._world_frame` detects the board on `overview_cam`/`handover_cam`, writes `T_world_cam` into the twin and persists it under `calib/extrinsics/`. A **top-down world-map visualization** also landed (`core/viz/scene.py::world_scene`/`scene_svg`, `/api/worldmodel/scene[.svg]`, `frontend/public/worldmap.html` + `WorldMapTab.vue`, `test_scene.py`) that renders both fixed cameras in the *same* solved frame — a showable proof the shared frame works. **Still TODO / placeholder:** on-arm gripper-cam `hand_eye`, `arm_to_arm`, and the scan step remain `TODO`; the board's real tag spacing is **still** an unmeasured `TODO(measure)` (`BOARD_SPACING_M = 0.060` placeholder). Minimum real calibration for the demo = **measure the board spacing** (the code is now committed; taught poses cover arm accuracy). | OPEN (world-frame COMMITTED; board spacing + hand-eye/scan TODO) | Dale / Di | The geometry-only verifiers (`tube_aligned`, `cap_removed`-separation) — the honest fallback for the hollow parent-based verify moment — are now world-correct **in code**; they become world-correct **in metric reality** once the 210/211 spacing is measured. One caliper measurement from trustworthy. |
| Q-ALLOC-1 | **Partially returned this cycle.** Last cycle the four-review laptop-over-room pattern looked broken (poses unstalled, kinematics committed). This cycle it re-emerged in a softer form: the genuinely-new work is the ratchet-unscrew + path-teaching (Q-CAPOPS-1, Q-COMMIT-2) — real, on-theme *dexterity* code, but **laptop/bench-buildable and uncommitted**, while the two room-only deciding items **stood still**: the OT serial merge (Q-OT-1, 6th cycle) and pose-teaching (Q-POSES-1, no new poses this cycle). The distinction is now sharp: the team keeps producing the buildable-anywhere half and defers the merge-and-teach-at-the-bench half. Does the next block go to the OT rig and the pose library rather than more manipulation code? | OPEN (strategic, **re-emerged**) | Team / Lukas | **Back near the top, alongside Q-OT-1.** The dexterity ceiling rose, but the demo's climax (OT draw) and its enabling bench work did not. Point the next block at the OT rig + finishing poses; commit the WIP; treat further manipulation code as displacement until the room work moves. |
| Q-FIDUCIAL-IDS | `MARKER_MAP` now uses **real** printed stock ids (`tag36h11` 180–224, confirmed from lab stock) — the id half is resolved. **Still open:** every entry keeps a placeholder `identity()` marker→entity offset (0.02) — "replace offsets with measured values" — so poses are numerically placeholder. When/who measures the offsets? | OPEN (ids ANSWERED, offsets OPEN) | Di / Dale | Fiducial-derived twin poses stay wrong until offsets are measured — blocks trusting perception for any verifier (Q-VERIFY-1). |
| Q-CAM-1 | Depth source is decided: `drivers/camera/realsense.py` (committed `368efba`) treats the three fixed viewpoints as RealSense RGB-D; a **still-image replay driver** (`drivers/camera/still.py`, `aa83f21`) backs any viewpoint whose hardware is temporarily unplugged; and **new this cycle**, a **remote camera driver** (`drivers/camera/remote.py`, `39bdca6`, registered as `"remote"`) serves another backend's cameras over its MJPEG endpoint — selected wholesale by `HZ_CAMERA_HOST`, so a second machine with no cameras plugged in can run the whole stack against the bench's viewpoints (frames enter at the driver layer, so detectors/fusion/twin all work unchanged; RGB-only so `has_depth` is False, honestly flagged `live: True` and errors on a frozen stream — `test_camera_remote.py`). Remaining: is depth actually used for the demo, or is RGB-only fiducial pose sufficient (per the CUT of FoundationPose)? | OPEN (source + 3 fallbacks ANSWERED; depth-use undecided) | Di | Fiducial pose works RGB-only; RGB-D is latent capacity unless a depth-using verifier is built. The still + remote drivers are solid demo hedges (dev on a second machine, survive an unplug), but a verifier must never read a replayed still as a live observation (the driver marks it; downstream must honor the mark). |
| Q-BRANCH-1 | The team's feature work has now **converged onto `agent-loop-p0`** (commits `2b0ed34`..`e641f56` landed the whole perception/verify/workflow stack here), so the demo branch is real. **Still open:** `origin/feat/ot-one-serial-driver` (the OT transport, Q-OT-1) is **not yet merged** into `agent-loop-p0`, and `origin` still carries `poc-depth-camera-preflight`, `fix/land-tip-pickup`, `main`, and backups. Where/when does `feat/ot-one-serial-driver` merge before freeze? | OPEN (multi-cycle) | Lukas | Merge risk narrowing but the one that matters (OT serial) is still outstanding. **No team commit landed this cycle** — HEAD's parent is still `8a52ec4`. **Still uncommitted, and grown:** the reparent WIP (Q-TWIN-COUPLING) plus a whole new batch — ratchet-unscrew, path-teaching, tests, teach-API + frontend (Q-CAPOPS-1, Q-COMMIT-2) — all sit in the working tree. Commit the batch with the pose work so a clean checkout has the grasp coupling and the dexterity tools. **PM doc commits still cannot be pushed from the sandbox:** no GitHub credentials, so a plain `git push` aborts with `could not read Username for 'https://github.com'` (auth/environment failure, *not* remote-ahead, so the `pull --rebase` retry does not apply). Upstream `origin/agent-loop-p0` is correctly set, so `git push` will succeed once a human runs it. Local PM doc commits (through this run's) are preserved, unpushed — a human should `git push` from a credentialed checkout. |
| Q-CLAUDE-1 | `ClaudePolicy` is gated on `ANTHROPIC_API_KEY`; `RuleBasedPolicy` is the default. Is the *agent-driven* (vs rule-based) decision loop in-scope for the demo narrative, or is rule-based the demo path with Claude as a stretch? | OPEN | Lukas | Scope + LLM-latency risk vs narrative punch. |
| Q-DEMO-1 | What is the deliberate **failure-injection** cue for the demo (which step, what perturbation)? The Track C "verify → retry / stop" punch is only compelling if a failure is shown on cue — depends on Q-VERIFY-1. | OPEN | Team | Narrative-critical; needs one verifier that can fail. |
| Q-RACK-1 | Real tube-rack / nest CAD still missing; `WELL_PITCH` provisionally 30 mm to clear the Ø28 mm 50 mL tube. | OPEN | Dale | Fixes present/aspirate alignment; provisional pitch may not match the physical rack. |
| Q-DEPLOY-1 | **NEW this run.** `8a52ec4` **removed** the Docker infra (`docker-compose.yml`, `docker/backend.Dockerfile`, `docker/frontend.Dockerfile`, `.dockerignore`). The stack now runs directly (backend `uv`/FastAPI + frontend `vite`), matching how the bench is operated. Confirmed deliberate, or does the demo/hand-off need a reproducible container? | NEW-this-run (low priority) | Lukas | Fine for a hackathon run-on-the-bench; only a concern if a judge or teammate needs a one-command reproducible bring-up. No demo-path impact. |
| Q-JLIMIT-1 | The J5 wrist-vs-flange-camera collision (controller error 31) is guarded in software, and **both arms now carry the override** (`FLANGE_CAM_J5_LIMITS = {"5": [-78.4, 95.0]}`) — both have a flange camera, so the same clearance class applies. **Still open:** the value was measured on **one** arm only (the .13 control box, 2026-07-25 at J3≈-108°, 5° margin) and is reused for the other by decision, not measurement — if the two camera mounts differ, the unmeasured arm's real clearance is unknown. The value is also J3-dependent, so it needs re-measuring if work moves to a markedly more folded J3. Who re-runs `scripts/find_joint_limit.py` on the second arm before the arm-held aspirate? | OPEN (one arm measured, one assumed) | Dale | A mount difference between the two flange cameras would make the reused limit too permissive on the unmeasured arm; blocks trusting a two-arm present/handover. |
| Q-FUSE-1 | The perception→twin fusion loop (`TwinFuser` + `services/twin_fusion` @10 Hz, started in `main.py`) is real and unit-tested, but it produces correct *world* coordinates only once calibration has placed the cameras — and the fixed-camera world-frame is now **committed** (`cfa8aac`) but the board spacing is still unmeasured and hand-eye is still `TODO` (Q-CALIB-1). Has fusion been run against a **real camera + real calibration** end-to-end, or only in unit tests / camera-frame mode? A new `core/perception/projection.py` (twin→image overlay) and `shapes.py` (Hough-circle detection of untagged labware) now also depend on the same camera world-poses. | OPEN | Di | Fusion + projection feeding the verifiers/overlay are only as trustworthy as the camera poses they read from the twin; until calibration is real, fused positions are in the camera frame, not the world frame. |

## Resolved / stale

| ID | Question | Status | Evidence |
|----|----------|--------|----------|
| Q-COMMIT-1 | The wired `_execute`, the four real verifiers, `TwinFuser`, the `twin_fusion` loop, the RealSense driver, `projection`/`shapes`/`capture`, `teach_poses`, and all their tests were **uncommitted WIP** (HEAD was `3800c2c`, shipping stubs + empty `_execute`). Who commits it, and when? | ANSWERED (2026-07-26T01:42Z) | **Resolved — the team committed the whole stack this cycle.** Commits `2b0ed34` (perception capture/projection/shapes/fusion), `4c6573a` (twin fusion service), `409f562` (real verifiers), `ae94f94` (wired `_execute`), `368efba` (RealSense driver + camera UI), `e31a7fc`/`69168e4` (teach checklist + tests), `a7c10c8`/`5df1dba`/`ed7c87b`/`e641f56` (config/docs/joint-limit recovery). HEAD is now `e641f56`; a clean checkout of `agent-loop-p0` runs the real verifiers + wired motion. Verified: all hero modules present in HEAD and `py_compile`-clean; `agents.py` contains zero `"stub"` strings. |
| Q-EXEC-1 | Who wires the **floor path** (snap-cap uncap → present → OT aspirate) into `uncap_aspirate.py::_execute` with taught poses + volumes? (Last named code blocker, empty for 4 cycles.) | ANSWERED (2026-07-26), committed `ae94f94` | `_execute` runs a data-driven `CHOREOGRAPHY` table via `_run_act`: real capability calls (`move_joints`/`move_to` gated by `check_joint_target`/`check_pose_target`, `grip`/`release`, OT `aspirate`) replaying **taught poses**; a loud `preflight` fails on any untaught pose before motion; `run()` executes→verifies→retries. `backend/tests/test_workflow_execute.py` asserts both arms move + grip, OT is called, and a missing pose raises. Now committed (was on-disk WIP last cycle). Carried-forward caveats live in Q-OT-1 + Q-POSES-1. |
| Q-PRIORITY-1 | For three cycles the team built *around* two named blockers (stub verifiers, empty `_execute`). Does the next block **commit** the WIP rather than open new substrate? | ANSWERED (2026-07-26T01:42Z) | **Fully resolved.** Both blockers went real *and* got committed (see Q-COMMIT-1). The prioritization gap the reviews hammered is closed in code and in git; remaining work is hardware bring-up (Q-OT-1, Q-POSES-1), not new subsystems. |
| Q-CAMERAS-MOUNT | `cameras.py` + `services/camera_hub.py` exist, but `main.py` never `include_router(cameras.router)`. | ANSWERED (2026-07-26), committed `368efba` | `main.py` now mounts `cameras.router`; per-camera detections stream on `/ws/state`. Camera-overlay demo + vision-verifier transport reachable; committed this cycle. |
| Q-VERIFY-1 | Which single verifier do we make real first, and from what signal? (Was the top blocker for 3 cycles: all four agents returned `ok=True, confidence=0.0, detail="stub"`.) | ANSWERED (2026-07-26) | **Resolved wholesale, not one — all four are now real (on-disk WIP).** `core/verification/agents.py`: `cap_removed` (cap reparented off tube + separation > 20 mm, +torque-drop telemetry bonus), `grasp_secure` (tube reparented onto a tool + gripper-width band), `tube_aligned` (distance to pipette nozzle < 15 mm), `aspiration_ok` (positive aspirated volume). Each returns graded `ok`/`confidence`/`detail`, degrades on missing signal, and has a dedicated `ok=False`/`ok=True` test in `backend/tests/test_verification.py`. Now committed (`409f562`) and, since `_execute` is wired + committed (Q-EXEC-1), the verdicts gate real motion; the residual is that they judge twin geometry until OT transport + real calibration land (Q-OT-1, Q-CALIB-1). |
| Q-CAD-1 | Are vendor CAD meshes for the exact tube/cap available? | ANSWERED (2026-07-25) | 15 mL + 50 mL tube/cap STLs under `assets/cad/tubes/`, registered in `core/worldmodel/meshes.py`, wired into `definitions.py::add_tube_with_cap` (default 50 mL). Satisfies FR-POSE-1 without BundleSDF. |
| Q-CONFIG-PATH | Where is the fleet declared? (doc said `backend/app/core/config.py`) | ANSWERED (2026-07-25) | Post-refactor it is `core/config.py` (`DEFAULT_FLEET`); `PROJECT_PLAN.md` corrected. |
| Q-RECOVERY-1 | May the recovery controller command both arms + OT, or arms only? | DEFERRED — not on demo critical path | Recovery controller is unbuilt (no code); revisit only if closed-loop recovery re-enters the demo scope. |
