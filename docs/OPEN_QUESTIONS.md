# Open questions

Running, de-duplicated, prioritized list owned by the PM reanalyze pass. Blockers /
critical-path first. Each item: **ID**, question, **status**, owner, and one-line impact.
Status ∈ `OPEN` · `NEW-this-run` · `ANSWERED`. Answered items are kept (not deleted) with
evidence, then archived below once stale.

_Last reanalyzed: 2026-07-26T01:09Z._

## Active (critical path first)

| ID | Question | Status | Owner | Impact |
|----|----------|--------|-------|--------|
| Q-COMMIT-1 | The wired `_execute`, the four real verifiers, `TwinFuser`, the `twin_fusion` loop, the RealSense driver, the new perception modules (`projection`/`shapes`/`capture`), `teach_poses`, and all their tests are **uncommitted WIP** — the last team code commit, HEAD (`3800c2c`), still ships the old `ok=True` stubs **and** the empty `_execute`. Who commits this onto the demo branch, and when? | OPEN (2 cycles) | Di / Lukas | **Now the #1 blocker.** Both named blockers are fixed *on disk* — but a clean checkout of HEAD is pure theater (stub verifiers + empty `_execute`). This cycle the entire working demo is invisible to git. Freeze must not happen on `3800c2c`. Committing is the single highest-leverage action left and writes no new code. |
| Q-EXEC-1 | Who wires the **floor path** (snap-cap uncap → present → OT aspirate) into `uncap_aspirate.py::_execute` with taught poses + volumes? (Was the last named blocker, empty for 4 cycles.) | ANSWERED (2026-07-26) | Lukas / Dale | **Resolved (on-disk WIP).** `_execute` now runs a data-driven `CHOREOGRAPHY` table via `_run_act`: real capability calls — `move_joints`/`move_to` gated by `check_joint_target`/`check_pose_target`, `grip`/`release`, and OT `aspirate` — replaying **taught poses** (no bench geometry hardcoded). A loud `preflight` fails on any untaught pose *before* anything moves, and `run()` executes→verifies→retries per step. `backend/tests/test_workflow_execute.py` asserts both arms actually move + grip and that a missing pose raises rather than silently skipping. Carried-forward caveats: OT transport is still a no-op (Q-OT-1) and it needs a taught bench (Q-POSES-1). |
| Q-PRIORITY-1 | For three cycles the team built *around* two named blockers (stub verifiers, empty `_execute`). Both are now fixed on disk — verifiers went real last cycle, `_execute` this cycle. Does the next block **commit** the WIP (Q-COMMIT-1) rather than open new substrate? | ANSWERED-in-substance / OPEN-on-commit | Team / Lukas | **Substantively resolved.** The prioritization gap the reviews hammered is closed in code: verification is real and now gates real motion. The *only* residual is mechanical — commit it. If the next block ships new subsystems instead of committing, the pattern has re-formed at the git boundary. |
| Q-POSES-1 | `_execute` replays 12 named taught poses (`tube_hold_approach` … `present_ot`); tests use a fixture library, but the real bench library (`data/teach_poses.json`, gitignored/bench-specific) is not in git and may be empty on the demo machine. `preflight` will now correctly *refuse to run* until every pose is taught. Who teaches the full choreography pose set on the real two-arm + OT bench, and when? | NEW-this-run | Dale / Lukas | **Demo-day blocker latent behind Q-EXEC-1.** The floor path is code-complete but will `preflight`-fail on an untaught bench — a green unit suite does not mean the demo will move. Teaching is a hands-on-hardware task that can't be done from a laptop. |
| Q-CAMERAS-MOUNT | `backend/app/api/cameras.py` (MJPEG `/api/cameras/{id}/stream` + `/detections`) and `services/camera_hub.py` exist, but `main.py` never `include_router(cameras.router)`. | ANSWERED (2026-07-26) | Di / Lukas | **Fixed.** `main.py:67` now `app.include_router(cameras.router)`; per-camera detections also stream on `/ws/state`. The camera-overlay demo and vision-verifier transport are reachable. |
| Q-OT-1 | OT-One driver `connect()`/`_send()` are **still** `TODO` (transport not opened): `connect()` sets `self._conn = object()` and `_send()` returns `None`. Now that `_execute` calls `ot.aspirate(...)` for real, this is the one wired motion that still **no-ops on hardware** — the pipette never actually draws. `origin/feat/ot-one-serial-driver` exists but is **not merged** into `agent-loop-p0`. | OPEN (rising) | Lukas | **The reveal is currently mimed.** With motion + verify now wired, the Opentrons aspiration is the narrative climax and the only step whose driver still does nothing physical — and `aspiration_ok` would then judge a twin/telemetry value with no real aspirate behind it. Merge/cherry-pick the serial driver or the reveal is a no-op with a green check. |
| Q-CALIB-1 | Calibration now runs end-to-end and publishes the twin, **but** `hand_eye` / `world_frame` / `arm_to_arm` / `locate_instruments` are still `TODO` (PlaceholderScanAdapter), so twin poses are placeholders. What is the **minimum** real calibration for the demo — taught poses only, or a real shared world frame from the fiducial board? | OPEN (was partly answered) | Dale / Di | Arm-held "present" accuracy and any twin-query verifier depend on real poses, not the placeholder scan. |
| Q-FIDUCIAL-IDS | `MARKER_MAP` now uses **real** printed stock ids (`tag36h11` 180–224, confirmed from lab stock) — the id half is resolved. **Still open:** every entry keeps a placeholder `identity()` marker→entity offset (0.02) — "replace offsets with measured values" — so poses are numerically placeholder. When/who measures the offsets? | OPEN (ids ANSWERED, offsets OPEN) | Di / Dale | Fiducial-derived twin poses stay wrong until offsets are measured — blocks trusting perception for any verifier (Q-VERIFY-1). |
| Q-CAM-1 | Depth source is now decided in code: `drivers/camera/realsense.py` (uncommitted WIP) treats **all three fixed cameras as RealSense RGB-D**. Remaining: is depth actually used for the demo, or is RGB-only fiducial pose sufficient (per the CUT of FoundationPose)? | OPEN (source ANSWERED) | Di | Fiducial pose works RGB-only; RGB-D is latent capacity unless a depth-using verifier is built. |
| Q-BRANCH-1 | HEAD `agent-loop-p0` **now tracks `origin/agent-loop-p0`** (upstream exists — the "no upstream" note from prior runs is resolved). `origin` also carries `feat/ot-one-serial-driver`, `poc-depth-camera-preflight`, `fix/land-tip-pickup`, `initial-setup-and-repo-structure`, `main`, and backups — work is still fanning out across branches with no convergence point. Where should everything (esp. `feat/ot-one-serial-driver`, Q-OT-1) merge before freeze? | OPEN (multi-cycle) | Lukas | Merge risk, growing. **PM doc commits still cannot be pushed — but now for a different reason:** the sandbox has **no GitHub credentials**, so a plain `git push` aborts with `could not read Username for 'https://github.com'` (an auth/environment failure, *not* a remote-ahead rejection, so the `pull --rebase` retry does not apply). The upstream is correctly set, so `git push` will succeed once a human runs it (or provides credentials). Local doc commits (`…5b5ff44`, `64ed6a3`, and earlier) are preserved, unpushed. |
| Q-CLAUDE-1 | `ClaudePolicy` is gated on `ANTHROPIC_API_KEY`; `RuleBasedPolicy` is the default. Is the *agent-driven* (vs rule-based) decision loop in-scope for the demo narrative, or is rule-based the demo path with Claude as a stretch? | OPEN | Lukas | Scope + LLM-latency risk vs narrative punch. |
| Q-DEMO-1 | What is the deliberate **failure-injection** cue for the demo (which step, what perturbation)? The Track C "verify → retry / stop" punch is only compelling if a failure is shown on cue — depends on Q-VERIFY-1. | OPEN | Team | Narrative-critical; needs one verifier that can fail. |
| Q-RACK-1 | Real tube-rack / nest CAD still missing; `WELL_PITCH` provisionally 30 mm to clear the Ø28 mm 50 mL tube. | OPEN | Dale | Fixes present/aspirate alignment; provisional pitch may not match the physical rack. |
| Q-JLIMIT-1 | The J5 wrist-vs-flange-camera collision (controller error 31) is guarded in software, and **both arms now carry the override** (`FLANGE_CAM_J5_LIMITS = {"5": [-78.4, 95.0]}`) — both have a flange camera, so the same clearance class applies. **Still open:** the value was measured on **one** arm only (the .13 control box, 2026-07-25 at J3≈-108°, 5° margin) and is reused for the other by decision, not measurement — if the two camera mounts differ, the unmeasured arm's real clearance is unknown. The value is also J3-dependent, so it needs re-measuring if work moves to a markedly more folded J3. Who re-runs `scripts/find_joint_limit.py` on the second arm before the arm-held aspirate? | OPEN (one arm measured, one assumed) | Dale | A mount difference between the two flange cameras would make the reused limit too permissive on the unmeasured arm; blocks trusting a two-arm present/handover. |
| Q-FUSE-1 | The perception→twin fusion loop (`TwinFuser` + `services/twin_fusion` @10 Hz, started in `main.py`) is real and unit-tested, but it produces correct *world* coordinates only once calibration has placed the cameras — and hand-eye/world-frame calibration is still `TODO` (Q-CALIB-1). Has fusion been run against a **real camera + real calibration** end-to-end, or only in unit tests / camera-frame mode? A new `core/perception/projection.py` (twin→image overlay) and `shapes.py` (Hough-circle detection of untagged labware) now also depend on the same camera world-poses. | OPEN | Di | Fusion + projection feeding the verifiers/overlay are only as trustworthy as the camera poses they read from the twin; until calibration is real, fused positions are in the camera frame, not the world frame. |

## Resolved / stale

| ID | Question | Status | Evidence |
|----|----------|--------|----------|
| Q-VERIFY-1 | Which single verifier do we make real first, and from what signal? (Was the top blocker for 3 cycles: all four agents returned `ok=True, confidence=0.0, detail="stub"`.) | ANSWERED (2026-07-26) | **Resolved wholesale, not one — all four are now real (on-disk WIP).** `core/verification/agents.py`: `cap_removed` (cap reparented off tube + separation > 20 mm, +torque-drop telemetry bonus), `grasp_secure` (tube reparented onto a tool + gripper-width band), `tube_aligned` (distance to pipette nozzle < 15 mm), `aspiration_ok` (positive aspirated volume). Each returns graded `ok`/`confidence`/`detail`, degrades on missing signal, and has a dedicated `ok=False`/`ok=True` test in `backend/tests/test_verification.py`. Caveat carried forward: they gate twin geometry, not physical reality, until `_execute` is wired (Q-EXEC-1), and the code is uncommitted (Q-COMMIT-1). |
| Q-CAD-1 | Are vendor CAD meshes for the exact tube/cap available? | ANSWERED (2026-07-25) | 15 mL + 50 mL tube/cap STLs under `assets/cad/tubes/`, registered in `core/worldmodel/meshes.py`, wired into `definitions.py::add_tube_with_cap` (default 50 mL). Satisfies FR-POSE-1 without BundleSDF. |
| Q-CONFIG-PATH | Where is the fleet declared? (doc said `backend/app/core/config.py`) | ANSWERED (2026-07-25) | Post-refactor it is `core/config.py` (`DEFAULT_FLEET`); `PROJECT_PLAN.md` corrected. |
| Q-RECOVERY-1 | May the recovery controller command both arms + OT, or arms only? | DEFERRED — not on demo critical path | Recovery controller is unbuilt (no code); revisit only if closed-loop recovery re-enters the demo scope. |
